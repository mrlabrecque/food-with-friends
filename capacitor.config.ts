import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'foodwithfriends',
  appName: 'food-with-friends',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
